// mapAPI

// url = `https://maps.googleapis.com/maps/api/place/photo?maxwidth=400&photoreference=PHOTOREF&key=${mapAPI}`

// fucntion intiMap () {
//     const
// }

// autocomplete
// `https://maps.googleapis.com/maps/api/place/autocomplete/json?input=${input}&types=(cities)&key=${mapAPI}`

// onChange={handleLocationSearch}

// fetch(`https://maps.googleapis.com/maps/api/place/autocomplete/json?input=${input}&types=(cities)&key=${mapAPI}`
// )

// .then(res => {
//     if (res.ok) {
//         res.json().then(data => data.map(predict => renderPredict(predict)))
//             }
//         }
//     )

// const renderPredict(predict)
